# minecraft-docker

Dockerised Minecraft Forge server for running locally or remote server.

# Requirements

- Docker

# Usage

```
docker compose up
```

When run locally, you can join the server over localhost over port 25565.
